#pragma once

/**
 * @brief Programa que dada una cadena y un desplazamiento, aplica el cifrado
 * cesar para encriptar el mensaje
 * @param texto la cadena (arreglo de caracteres) a cifrar
 * @param desplazamiento el desplazamiento a aplicar en el cifrado
 */
void cifrar_cadena(char *texto, int desplazamiento);
